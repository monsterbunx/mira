{
  "name": "mira",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch server.ts",
    "start": "bun run server.ts",
    "db:push": "bunx prisma db push --skip-generate --accept-data-loss",
    "db:generate": "bunx prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0"
  },
  "devDependencies": {
    "prisma": "^5.22.0",
    "@types/bun": "latest"
  }
}
